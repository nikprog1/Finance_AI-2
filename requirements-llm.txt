# Cloud LLM (OpenRouter/OpenAI). Dependencies are in main requirements.txt
# Install: pip install -r requirements.txt (includes httpx, python-dotenv)
# Configure: copy .env.example to .env.local, set LLM_API_KEY
